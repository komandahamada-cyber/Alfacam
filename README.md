
# Alfacam - دليل تشغيل المشروع

تطبيق Alfacam هو تطبيق ويب متطور (PWA) لقراءة الروشتات والتحاليل الطبية باستخدام الذكاء الاصطناعي.

## 📁 هيكل المشروع
للحفاظ على المشروع وتعديله لاحقاً، تأكد من وضع الملفات بنفس الترتيب التالي:
- `/` (المجلد الرئيسي)
  - `index.html`
  - `index.tsx`
  - `App.tsx`
  - `types.ts`
  - `manifest.json`
  - `metadata.json`
  - `package.json`
  - `vite.config.ts`
  - `/components`
    - `CameraView.tsx`
    - `ProcessingView.tsx`
    - `ResultView.tsx`
    - `CropView.tsx`
    - `HistoryView.tsx`
  - `/services`
    - `geminiService.ts`

## 🚀 كيفية التشغيل محلياً

### الطريقة الأولى: السريعة (Live Server)
1. افتح المجلد باستخدام برنامج **VS Code**.
2. قم بتثبيت إضافة (Extension) تسمى **Live Server**.
3. اضغط كليك يمين على ملف `index.html` واختار **Open with Live Server**.

### الطريقة الثانية: الاحترافية (للتعديل والتطوير)
1. تأكد من تثبيت **Node.js** على جهازك.
2. افتح "موجة الأوامر" (Terminal) داخل مجلد المشروع.
3. اكتب الأمر التالي لتثبيت الأدوات:
   ```bash
   npm install
   ```
4. لتشغيل البرنامج في وضع التطوير:
   ```bash
   npm run dev
   ```

## 🛠 التعديل على الكود
- لتغيير الألوان أو التصميم: ابحث في ملفات المكونات داخل مجلد `components`.
- لتغيير منطق الذكاء الاصطناعي: اذهب إلى `services/geminiService.ts`.
- لتغيير اسم التطبيق أو الأيقونة: عدل ملف `manifest.json`.

## ⚠️ ملاحظة هامة
البرنامج يحتاج إلى **API KEY** من Google Gemini. عند التشغيل محلياً، يجب إعداد مفتاحك الخاص في ملف يسمى `.env`.

---
**تم التطوير بواسطة: Alfacam Team**
